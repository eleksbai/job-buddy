[project]
name = "job-buddy"
version = "0.1.0"
description = "Local job hunting assistant with FastAPI, FastMCP, and MongoDB."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "fastmcp>=3.2.4",
    "motor>=3.7.1",
    "patchright>=1.59.1",
    "pydantic-settings>=2.9.1",
    "pymongo>=4.13.0",
    "pytest>=9.0.3",
    "python-dotenv>=1.1.0",
    "python-multipart>=0.0.20",
    "uvicorn>=0.34.2",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.28.1",
    "pre-commit>=4.2.0",
    "pytest>=8.3.5",
    "pytest-asyncio>=1.0.0",
]

[project.scripts]
job-buddy-mcp = "job_buddy.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/job_buddy"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "ipython>=9.13.0",
]
